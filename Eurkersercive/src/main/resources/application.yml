server:
  port: 8761
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # 注册中心地址
    register-with-eureka: false # 是否将自己进行注册
    fetch-registry: false # 是否从其他注册中心导入数据
      